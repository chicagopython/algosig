## -*- coding: utf-8 -*-
<%namespace name="helper" file="post_helper.tmpl"/>
<%namespace name="pheader" file="post_header.tmpl"/>
<%namespace name="comments" file="comments_helper.tmpl"/>
<%namespace name="math" file="math_helper.tmpl"/>
<%inherit file="post.tmpl"/>
<%block name="content">
<div class="hero-image">
  <div class="hero-text">
    <h1 style="font-size:50px">Chicago Python User Group</h1>
    <p>Python Project Night</p>
    <button>Third Thursdays Braintree</button>
  </div>
</div>
<article class="post-${post.meta('type')}" itemscope="itemscope" itemtype="http://schema.org/Article">
  %if not post.meta('hidetitle'):
    <header>
        ${pheader.html_title()}
        ${pheader.html_translations(post)}
    </header>
  %endif
    <div class="e-content entry-content" itemprop="articleBody text">
    ${post.text()}
    </div>
    %if site_has_comments and enable_comments and not post.meta('nocomments'):
        <section class="comments">
        <h2>${messages("Comments")}</h2>
        ${comments.comment_form(post.permalink(absolute=True), post.title(), post.base_path)}
        </section>
    %endif
    ${math.math_scripts_ifpost(post)}
</article>
</%block>
